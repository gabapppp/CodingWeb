from django.db import models

"""
class Source(models.Model):
    source_code = models.TextField()
    language = models.CharField(max_length=255, blank=False, default='python')
    stdin = models.CharField(blank=True, max_length=255)
"""
